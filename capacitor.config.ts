import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.neurafin.hub',
    appName: 'NeuraFin Hub',
    webDir: 'out',
    bundledWebRuntime: false
};

export default config;
